# ─── SERVER ───
PORT=5000
NODE_ENV=development

# ─── DATABASE ───
# Leave MONGODB_URI blank/unset for demo mode. Set it for full MongoDB-backed functionality.
MONGODB_URI=mongodb://localhost:27017/medihub
# For MongoDB Atlas (production):
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/medihub

# ─── JWT ───
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=7d

# ─── ANTHROPIC AI ───
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ─── STRIPE PAYMENTS ───
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ─── EMAIL (Nodemailer) ───
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
EMAIL_FROM=MediHub <noreply@medihub.com>

# ─── FRONTEND URL (for CORS) ───
# Use a comma-separated list for multiple origins, or * during local demo work.
CLIENT_URL=http://localhost:5500

# ─── FILE UPLOADS ───
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads
